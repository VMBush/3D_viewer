#include "glview.h"

glView::glView() {
    
}

glView::glView(QWidget* w) : QOpenGLWidget(w), glView(){

}


void glView::initializeGL() {
    glMatrixMode(GL_PROJECTION);
    
}

void glView::paintGL() {

}

Qstring glView::getFromConf(Qstring param) {
    std::ifstream file("options.conf");
    QString line;
    
    while (std::getline(file, line)) {
        QStringList words = line.split("")
    }
    
    file.close();
}
